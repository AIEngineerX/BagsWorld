# =============================================================================
# BAGSWORLD CONFIGURATION
# =============================================================================

# Bags.fm API Configuration
# Get your API key from Bags.fm
BAGS_API_KEY=your_bags_api_key_here
BAGS_API_URL=https://public-api-v2.bags.fm/api/v1

# Solana Configuration
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta

# =============================================================================
# ECOSYSTEM CONFIGURATION
# =============================================================================
# These control the BagsWorld ecosystem mechanics

# Ecosystem treasury wallet - receives 5% of all token launch fees
# This funds development, community rewards, and future token liquidity
# View live at: https://solscan.io/account/9Luwe53R7V5ohS8dmconp38w9FoKsUgBjVwEPPU8iFUC
NEXT_PUBLIC_ECOSYSTEM_WALLET=9Luwe53R7V5ohS8dmconp38w9FoKsUgBjVwEPPU8iFUC

# Admin wallet - can delete buildings and moderate the world
NEXT_PUBLIC_ADMIN_WALLET=9Luwe53R7V5ohS8dmconp38w9FoKsUgBjVwEPPU8iFUC

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# AI Agent Configuration (Optional)
# If not provided, the AI will use rule-based responses
# Get your API key from: https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# AUTONOMOUS AGENT CONFIGURATION
# =============================================================================
# For autonomous fee claiming and trading operations
# WARNING: This wallet will sign transactions automatically!

# Agent Wallet Private Key (Base58 encoded)
# Generate a new wallet for the agent, NOT your main wallet
# To generate: solana-keygen new --no-bip39-passphrase -o agent-wallet.json
# Then convert to base58: cat agent-wallet.json | node -e "console.log(require('bs58').encode(Buffer.from(JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')))))"
AGENT_WALLET_PRIVATE_KEY=your_agent_wallet_base58_private_key

# Agent API Secret (Required for production)
# Used by GitHub Actions to authenticate with the agent API
# Generate with: openssl rand -hex 32
AGENT_SECRET=your_random_secret_here

# Agent Configuration (Optional - defaults shown)
# AGENT_MIN_CLAIM_THRESHOLD=0.01
# AGENT_CHECK_INTERVAL_MS=300000
# AGENT_MAX_CLAIMS_PER_RUN=10

# =============================================================================
# NEON DATABASE CONFIGURATION
# =============================================================================
# For shared global building state across all users
# On Netlify: Auto-configured via Neon integration (sets NETLIFY_DATABASE_URL)
# Self-hosted: Set DATABASE_URL manually
#
# Get connection string from: https://console.neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Supabase Configuration (Legacy - Neon is now preferred)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_KEY=your_supabase_anon_key_here

# =============================================================================
# X (TWITTER) CONFIGURATION
# =============================================================================
# For daily reports and X authentication
# 1. Go to https://developer.twitter.com/en/portal/dashboard
# 2. Create a new Project and App (Free tier works for auth, Pro for posting)
# 3. In App Settings > User authentication settings:
#    - Enable OAuth 2.0
#    - Type: Web App
#    - Callback URLs: http://localhost:3000/api/auth/x/callback (dev)
#                     https://yourdomain.com/api/auth/x/callback (prod)
#    - Website URL: Your site URL
# 4. Copy Client ID and Client Secret
# NOTE: Posting tweets requires X API Pro/Basic plan ($100/mo)

X_CLIENT_ID=your_x_client_id_here
X_CLIENT_SECRET=your_x_client_secret_here
NEXT_PUBLIC_X_CALLBACK_URL=http://localhost:3000/api/auth/x/callback

# Site URL (for internal API calls in daily reports)
NEXT_PUBLIC_SITE_URL=https://bagsworld.netlify.app
